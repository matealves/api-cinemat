- DB: 
    - users:
        name, lastName, email, password

    - movies:
        name, description, poster, schedules:
                                        hour, room, sents: * 3
                                                        number, available * 120

- API:
    - CRUD users + login + autentication
    - CRUD movies
